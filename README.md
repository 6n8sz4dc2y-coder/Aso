# PitWall Q3 Prototype

Static prototype built from `Weekly update.xlsx`. Open `index.html` locally or deploy to GitHub Pages / Vercel.

Files:
- `index.html` dashboard
- `data.js` extracted Q3 data
- `q3_dashboard_data.csv` registration data
